-- config/layouts.lua — meloworld
-- Master-stack and dwindle layout settings.

hl.config({
	general = {
		layout = "dwindle",
	},

	dwindle = {
		preserve_split = true,
		smart_split = false,
		smart_resizing = true,
		force_split = 2,
	},
})
